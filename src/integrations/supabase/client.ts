// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import PostgrestClient from '@supabase/postgrest-js';
import type { Database } from './types';

// Import from Vite env (must be prefixed with VITE_)
const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL as string;
const SUPABASE_PUBLISHABLE_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY as string;
// Optional: direct Postgrest URL, used only when the gateway can't reach
// Postgrest (e.g. the Railway Envoy routing issue). Leave unset elsewhere.
const SUPABASE_REST_URL = import.meta.env.VITE_SUPABASE_REST_URL as string | undefined;

// Optional: fail fast if missing
if (!SUPABASE_URL || !SUPABASE_PUBLISHABLE_KEY) {
	console.error('Missing Supabase env vars. Check VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.');
}

export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY);

if (SUPABASE_REST_URL) {
	(supabase as unknown as { rest: unknown }).rest = new PostgrestClient(SUPABASE_REST_URL, {
		headers: (supabase as unknown as { headers: Record<string, string> }).headers,
		schema: 'public',
		fetch: (supabase as unknown as { fetch: typeof fetch }).fetch,
	});
}